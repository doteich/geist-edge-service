apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: geist-operator
  name: geist-operator-geistconnector-admin-role
rules:
- apiGroups:
  - geistconnectors.config.geist-iot.com
  resources:
  - geistconnectors
  verbs:
  - '*'
- apiGroups:
  - geistconnectors.config.geist-iot.com
  resources:
  - geistconnectors/status
  verbs:
  - get
