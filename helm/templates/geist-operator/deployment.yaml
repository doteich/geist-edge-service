apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: geist-operator
    control-plane: controller-manager
  name: geist-operator-controller-manager
  namespace: {{ .Release.Namespace }}
spec:
  replicas: {{ .Values.geistOperator.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: geist-operator
      control-plane: controller-manager
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: manager
      labels:
        app.kubernetes.io/name: geist-operator
        geist-application-type: geist-operator
        control-plane: controller-manager
    spec:
      containers:
      - args:
        - --metrics-bind-address=:8443
        - --leader-elect
        - --health-probe-bind-address=:8081
        command:
        - /manager
        image: "{{ .Values.geistOperator.image.repository }}:{{ .Values.geistOperator.image.tag }}"
        imagePullPolicy: {{ .Values.geistOperator.image.pullPolicy }}
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 20
        name: manager
        ports: []
        env:
          - name: OPERATOR_NAMESPACE
            valueFrom:
              fieldRef:
                fieldPath: metadata.namespace
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: {{ .Values.geistOperator.resources.limits.cpu }}
            memory: {{ .Values.geistOperator.resources.limits.memory }}
          requests:
            cpu: {{ .Values.geistOperator.resources.requests.cpu }}
            memory: {{ .Values.geistOperator.resources.requests.memory }}
        securityContext:
          allowPrivilegeEscalation: {{ .Values.geistOperator.securityContext.allowPrivilegeEscalation }}
          capabilities:
            drop:
{{ toYaml .Values.geistOperator.securityContext.capabilities.drop | indent 12 }}
          readOnlyRootFilesystem: {{ .Values.geistOperator.securityContext.readOnlyRootFilesystem }}
      securityContext:
        runAsNonRoot: {{ .Values.geistOperator.runAsNonRoot }}
        seccompProfile:
          type: {{ .Values.geistOperator.seccompProfile.type }}
      serviceAccountName: geist-operator-controller-manager
      terminationGracePeriodSeconds: 10
      volumes: []