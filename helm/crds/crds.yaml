apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: geistconnectors.config.geist-iot.com
spec:
  group: config.geist-iot.com
  names:
    kind: GeistConnector
    listKind: GeistConnectorList
    plural: geistconnectors
    singular: geistconnector
  scope: Namespaced
  versions:
  - name: v1alpha
    schema:
      openAPIV3Schema:
        description: GeistConnector is the Schema for the geistconnectors API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GeistConnectorSpec defines the desired state of GeistConnector
            properties:
              connectorSpec:
                description: |-
                  OpcuaSpec holds the application configuration.
                  It now contains both Opcua and Redpanda configs as siblings.
                properties:
                  opcua:
                    description: OpcuaConfig reflects the 'opcua' block
                    properties:
                      connection:
                        description: ConnectionConfig reflects the 'connection' block
                        properties:
                          authentication:
                            description: AuthenticationConfig reflects the 'authentication'
                              block
                            properties:
                              certificate:
                                description: AuthCertConfig reflects the 'authentication.certificate'
                                  block
                                properties:
                                  certificate:
                                    type: string
                                type: object
                              credentials:
                                description: CredentialConfig reflects the 'credentials'
                                  block
                                properties:
                                  password:
                                    type: string
                                  username:
                                    type: string
                                type: object
                              type:
                                enum:
                                - None
                                - User&Password
                                - Certificate
                                type: string
                            required:
                            - type
                            type: object
                          certificate:
                            description: CertificateConfig reflects the 'connection.certificate'
                              block
                            properties:
                              auto_create:
                                default: true
                                type: boolean
                              certificate:
                                type: string
                              external_certificate:
                                default: false
                                type: boolean
                              private_key:
                                type: string
                            type: object
                          endpoint:
                            type: string
                          mode:
                            enum:
                            - None
                            - Sign
                            - SignAndEncrypt
                            type: string
                          policy:
                            enum:
                            - None
                            - Basic256
                            - Basic256Sha256
                            - Aes256Sha256RsaPss
                            - Aes128Sha256RsaOaep
                            type: string
                          port:
                            format: int32
                            type: integer
                          retry_count:
                            format: int32
                            type: integer
                        required:
                        - authentication
                        - certificate
                        - endpoint
                        - mode
                        - policy
                        - port
                        - retry_count
                        type: object
                      subscription:
                        description: SubscriptionConfig reflects the 'subscription'
                          block
                        properties:
                          nodeids:
                            items:
                              description: NodeIDInfo reflects an element in the 'nodeids'
                                list
                              properties:
                                id:
                                  type: string
                              required:
                              - id
                              type: object
                            type: array
                          sub_interval:
                            format: int32
                            type: integer
                        required:
                        - nodeids
                        - sub_interval
                        type: object
                    required:
                    - connection
                    - subscription
                    type: object
                  redpanda:
                    description: RedpandaConfig reflects the 'redpanda' block
                    properties:
                      auth:
                        description: RedpandaAuthConfig reflects the 'auth' block
                        properties:
                          sasl:
                            description: SASLConfig reflects the 'sasl' block
                            properties:
                              password:
                                type: string
                              type:
                                enum:
                                - plain
                                - scram-sha-256
                                - scram-sha-512
                                type: string
                              user:
                                type: string
                            required:
                            - password
                            - type
                            - user
                            type: object
                        required:
                        - sasl
                        type: object
                      brokers:
                        items:
                          type: string
                        type: array
                      tls:
                        description: RedpandaTLSConfig reflects the 'tls' block
                        properties:
                          insecure_skip_verify:
                            type: boolean
                        type: object
                      topic:
                        type: string
                    required:
                    - brokers
                    - topic
                    type: object
                required:
                - opcua
                - redpanda
                type: object
              deploymentSpec:
                description: DeploymentSpec contains Kubernetes-specific configuration
                  for the application's deployment.
                properties:
                  customAnnotations:
                    additionalProperties:
                      type: string
                    description: Map of custom annotations to add to the Deployment/Pod.
                    type: object
                  customLabels:
                    additionalProperties:
                      type: string
                    description: Map of custom labels to add to the Deployment/Pod.
                    type: object
                  imageRepo:
                    default: doteich/geist-connector
                    description: The container image repository (e.g., myregistry/collector).
                    type: string
                  imageVersion:
                    default: latest
                    description: The container image version/tag (e.g., v1.0.0).
                    type: string
                  pullPolicy:
                    default: IfNotPresent
                    description: The image pull policy.
                    enum:
                    - Always
                    - IfNotPresent
                    - Never
                    type: string
                required:
                - imageRepo
                - imageVersion
                - pullPolicy
                type: object
            required:
            - connectorSpec
            - deploymentSpec
            type: object
          status:
            description: GeistConnectorStatus defines the observed state of GeistConnector
            properties:
              conditions:
                description: Conditions represents the latest available observations
                  of the resource's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}